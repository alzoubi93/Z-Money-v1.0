import type { CapacitorConfig } from "@capacitor/cli";
const config: CapacitorConfig = {
 appId: "com.zmoney.family",
 appName: "Z.Money",
 webDir: "dist",
 bundledWebRuntime: false,
 android: { backgroundColor: "#f5f7fb" }
};
export default config;